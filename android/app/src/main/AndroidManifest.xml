<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!--
      Los dos permisos de almacenamiento quedan acotados a las versiones donde
      todavía hacen algo. Desde Android 13 (API 33) la fototeca se pide con
      READ_MEDIA_IMAGES, y WRITE_EXTERNAL_STORAGE no concede nada desde Android
      10 (API 29). Sin el maxSdkVersion, Play los ve declarados como permisos de
      almacenamiento amplio y pregunta por ellos en la ficha.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- Camera is not strictly required for the app to run, but needed for profile photo -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="Legacy app"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Vuelta desde la pasarela de pagos.
                 Sin esto, CredibanCo redirigia a legacyapp://... y el navegador
                 no encontraba ninguna aplicacion que atendiera ese esquema: el
                 usuario se quedaba en una pantalla de error DESPUES de haber
                 pagado, y el cobro no se confirmaba nunca.
                 El host "app" es necesario: Flutter enruta por el PATH de la
                 URI, y legacyapp://payment-callback deja el path vacio. Con
                 legacyapp://app/payment-callback el path es el que go_router
                 espera. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="legacyapp" android:host="app"/>
            </intent-filter>
        </activity>

        <!-- Deja que el motor de Flutter entregue la URI entrante al router en
             vez de limitarse a abrir la app en la pantalla inicial. -->
        <meta-data
            android:name="flutter_deeplinking_enabled"
            android:value="true"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!--
          Sin estas dos declaraciones, en Android 11+ `canLaunchUrl` devuelve
          false aunque haya navegador: desde API 30 una app solo "ve" las que
          declara aquí. Nueve puntos de la app preguntan antes de abrir, asi que
          el bloque de arriba —el que trae Flutter por defecto— dejaba muertos
          TODOS los enlaces externos: los documentos legales que exigen las
          tiendas, la pasarela de pago, los articulos, los videos y la tienda de
          LSO. Comprobado en un telefono el 2026-08-20.
        -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
